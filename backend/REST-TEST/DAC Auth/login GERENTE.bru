meta {
  name: login GERENTE
  type: http
  seq: 4
}

post {
  url: HTTP://localhost:8081/auth/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
    {
      "email": "GERENTE@bantads",
      "senha": "batata"
    }
}
