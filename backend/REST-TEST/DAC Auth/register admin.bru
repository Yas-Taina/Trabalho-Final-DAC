meta {
  name: register admin
  type: http
  seq: 6
}

post {
  url: HTTP://localhost:8080/auth/register
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
   {
      "email": "admin@bantads",
      "senha": "batata",
      "role": "ADMIN"
    }
  
  
}
