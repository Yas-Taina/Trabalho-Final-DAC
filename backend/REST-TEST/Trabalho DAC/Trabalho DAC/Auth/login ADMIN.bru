meta {
  name: login ADMIN
  type: http
  seq: 3
}

post {
  url: HTTP://localhost:8080/auth/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
    {
      "email": "admin@bantads",
      "senha": "batata"
    }
}
